name: Deploy AXE preview
# Dark source: feat/figma-concept-1 @ fb7912b7ea0b648ec07ac1821c7765ea95d0735d
# Light source: feat/figma-concept-light @ b5e69881f96e85352955b915c78c133dc468a319

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/deploy-preview.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout current dark AXE concept
        uses: actions/checkout@v4
        with:
          ref: feat/figma-concept-1

      - name: Checkout light AXE concept
        uses: actions/checkout@v4
        with:
          ref: feat/figma-concept-light
          path: _light_source

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Prepare static site
        shell: bash
        run: |
          set -euo pipefail
          rm -rf _site
          mkdir -p _site

          # Existing dark preview remains at the original URL.
          cp index.html _site/index.html
          cp -R assets _site/assets

          # Separate light preview; all asset URLs are path-isolated from dark.
          mkdir -p _site/light
          cp _light_source/index.html _site/light/index.html
          cp -R _light_source/assets _site/light/assets

          # Cache-isolated light fallback for this exact branch head.
          LIGHT_ALT_PATH="light-fresh-b5e69881"
          mkdir -p "_site/${LIGHT_ALT_PATH}"
          cp _light_source/index.html "_site/${LIGHT_ALT_PATH}/index.html"
          cp -R _light_source/assets "_site/${LIGHT_ALT_PATH}/assets"

          touch _site/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
